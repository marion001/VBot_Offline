pcm.!default {
    type asym
    playback.pcm "speaker"
    capture.pcm "mic"
}

pcm.mic {
    type plug
    slave.pcm "array"
}

pcm.speaker {
    type plug
    slave.pcm "dmixed"
}

pcm.dmixed {
    type dmix
    slave.pcm "hw:0,0"
    ipc_perm 0666
    ipc_key 555555
}

pcm.array {
    type dsnoop
    ipc_perm 0666
    slave {
        pcm "hw:0,0"
        channels 1
    }
    ipc_key 666666
}
